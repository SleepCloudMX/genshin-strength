# Genshin Strength

原神伤害计算与可视化。以梦见月瑞希的星扩散直伤为第一个例子，**一个需求一个需求做** ——
没有全局架构规划，攒够需求再考虑要不要系统化。

## 开工前先读

**[`docs/ai-output/memory.md`](docs/ai-output/memory.md)** —— 项目事实的唯一来源：
目录与 git 布局、命名约定、代码入口、核心口径（等效词条 / 双暴分 / 基准面板）、
文档导航表、环境、已知的坑、未决事项。

它不在这个仓库里（见下），但路径是死的。

## 唯一容易踩的坑

`docs/ai-output/`（产出文档）与 `docs/ai-ref/`（原始资料）是**独立的嵌套 git 仓库**，
被根仓库的 `docs/.gitignore` 忽略 —— **提交要分别提交**。

代码与产物在根仓库；`output/` 被 gitignore，是给人看的产物。
